js is dynamically typed lang
js is case sensetive
no space in variable name
only letter, underscore or $ is allowed in variable name and can be the first character
var - re-declare and update . global scope variable
let - cannot re-declare but can be updated . block scope variable
const - cannot re-declare and cannot update . block scope variable
 
 
                           data types
  1 primitive datatype  (7)   
  number,string,boolean,undefined,null,bigint,symbol                      
2 non-primitive datatype
objects  (key:value pair)